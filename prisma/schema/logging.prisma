model LogEntry {
    id        Int      @id @default(autoincrement())
    createdAt DateTime @default(now())

    message  String
    level    LogLevel
    metadata Json?
}

enum LogLevel {
    debug
    http
    info
    warn
    error
    fatal
}
