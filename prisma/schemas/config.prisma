model AppConfig {
    id Int @id @default(autoincrement())

    initialSetup Boolean @default(false)
}

model AppSettings {
    id Int @id @default(autoincrement())

    public Boolean @default(false)
}
